import React, { useState } from "react";
import { GlobalContext } from "../../GlobalContext";

const OrderHistory = () => {
  return (
    <div>
      <h2>주문 내역</h2>
      {/* 주문 내역 아이템 목록을 여기에 표시 */}
    </div>
  );
};

export default OrderHistory;
