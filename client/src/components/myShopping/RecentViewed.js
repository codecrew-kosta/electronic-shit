import React, { useState } from "react";
import { GlobalContext } from "../../GlobalContext";

const RecentViewed = () => {
  return (
    <div>
      <h2>최근 본 상품</h2>
      {/* 최근 본 상품 아이템 목록을 여기에 표시 */}
    </div>
  );
};

export default RecentViewed;
