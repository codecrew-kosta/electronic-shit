import React, { useState } from "react";
import { GlobalContext } from "../../GlobalContext";

const WishList = () => {
  return (
    <div>
      <h2>찜 목록</h2>
      {/* 찜 목록 아이템 목록을 여기에 표시 */}
    </div>
  );
};

export default WishList;
